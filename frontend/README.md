# Frontend (Vite + React) - Bank Document Verification Demo

## Setup
- Node.js (v16+)
- Install deps:
  ```bash
  cd frontend
  npm install
  ```

## Run
  ```bash
  npm run dev
  ```

The dev server runs on http://localhost:5173 by default.
